extends ../layouts/application.pug

block header
  = t('views.statuses.new.title')
block content
  +formFor()(action=route('createStatus') method='post')
    +input(status, 'name', errors)(type="text")
    +submit(t('views.statuses.new.create'))
